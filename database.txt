Описание базы данных...
Как водится, всё у нас не как у людей...Я думаю так....


таблица человека
create table human (id varchar(32), name, surname,givename, birthdate, deathdate, deathreason);
где хранить подробности и хранить ли?
ГДе хранить взаимодействие между ними всеми???
//таймстамп не катит, кстати, ну никак...а как же тогда хранить...неясно...год, день, месяц? Дата? как?
По идее мать и отец - константы и нечего их хранить в отдельной таблице...бессмысленно это...
таким образом..надо каждому ребёнку дать указатель на мать и отца...я думаю так...
А каждому отцу и матери - указатели на детей...не номера! А объекты! Объекты!!! Гм...ну а что, почему бы и нет...
Подожди-ка, подожди-ка..не надо так...
Или надо...
Вообще говоря, мы каждому классу присваиваем идентификатор, не больше и не меньше.
Поэтому они будут переходить в БД прямо с этими идентификаторами и ничего с ними не случится...
а идентификатор они смогут узнать друг от друга...так и только так...


05.04.2016

Таблица message
Ориентировочное количество сообщений - 500.
+1. Идентификатор.
+2. Дата (год, месяц, день отдельно, наверное можно).
+3. Тип (таблица с табуляторами, просто текст и всё такое прочее).
+4. Содержимое сообщения (Что там вообще в нём, ага).

CREATE TABLE IF NOT EXISTS records
(id VARCHAR(32) PRIMARY KEY NOT NULL,
    record TEXT,
    date TEXT,
    type INTEGER,
    source VARCHAR(32),
    location TEXT,
    owner VARCHAR(32)
);

Таблица населённых пунктов places.
Ориентировочное количество пунктов - 100.
+1. Идентификатор
+2. Название основное.
+3. Названия дополнительные через разделитель ; к примеру. Поиск в таком случае несколько ухудшается, но и ладно.
+4. Координаты (я так думаю, что текстовое поле с разделителем, там уже решим потом).
+5. Комментарии (пока это вместо принадлежности к уездам и тому подобному).
ПОтом дополним ещё тут или новую таблицу создадим.


CREATE TABLE IF NOT EXISTS places
(id VARCHAR(32) PRIMARY KEY NOT NULL,
    name TEXT,
    anothernames TEXT,
    coordinates TEXT,
    comments TEXT,
    owner VARCHAR(32)
);

Таблица людей humans
Ориентировочное количество людей - 1000 (вряд ли осилю больше, но попробую, чего уж). Набивать долго буишь.
+1. Идентификатор
+2. Фамилия
+3. Девичья Фамилия (для мужчин - альтернативная фамилия, такое тож бывает иногда).
+4. Имя
+5. Отчество
+6. Отец (идентификатор). По ним будем строить дерево.
+7. Мать (идентификатор). По ним будем строить дерево.
+8. Дата рождения (год/месяц/день отдельно?) да, скорее всего получится отдельно. или в каком-нибудь спец.формате.
вроде 12.01.2009 14:45. Думаю, что последнее, а именно текст, предпочтительно перед временем с начала эпохи.
У нас тут не лог, а серьёзная организация, ага. Так что окай, пробуем так.
-9. Тип даты рождения: точная, примерная, в свободной форме (просто текстовое поле и т.д.)
+9. Дата смерти.
-10. Типа даты смерти: точная, примерная, в свободной форме (просто текстовое поле и т.д.)
+11. Причина смерти.
+12. Место рождения (можно попробовать, да) или отдельно как-то это всё описать.
    ИДЕНТИФИКАТОР (там мы ему уже и название, и gps и всё-всё-всё).
+13. Пол.
+14. Род занятий.
15. Кто добавил. owner (необязательно в общем-то...можно даже не заполнять)

CREATE TABLE IF NOT EXISTS humans
(id VARCHAR(32) PRIMARY KEY NOT NULL,
    name TEXT,
    patrynomic TEXT,
    surname TEXT,
    marname TEXT,//таки выпилили maiden отовсюду.
    sex INTEGER,
    father VARCHAR(32),
    mother VARCHAR(32),
    birthdate TEXT,
    birthplace VARCHAR(32),
    deathdate TEXT,
    death reason TEXT,
    occupation TEXT,
    owner VARCHAR(32)
);
q.prepare("create table if not exists verbforms_es
    (id varchar(32) primary key not null,
    verb_id varchar(32),
    form text,
    searchform text,
    regularity integer,
    plurality integer,
    gender integer,
    pronoun integer,
    tense integer);");


Таблица с информацией, где человек побывал humanplaces
+1. Идентификатор человек.
+2. Идентификатор места.
+3. Год получения информации.
Точнее дата, точнее даже ПЕРИОД в данном случае...Ну а вооще дата.
Или период. Например: before 01.01.01 12:53 - after 01.01.02 12:51 Распарсил и доволен.
+4. Флаг: место жительства, место смерти и всё такое.

CREATE TABLE IF NOT EXISTS humanplaces
(id VARCHAR(32) PRIMARY KEY NOT NULL,//нужен идентификатор вообще или не нужен?
    humanid VARCHAR(32),
    placeid VARCHAR(32)
    date TEXT,
    type INTEGER,
    owner VARCHAR(32)
);

Таблица с информацией о браках marriages
Тут с датой аналогично. Если указан период, значит период. если не период, значит не период.
тип - ясное дело: официальный, не официальный. Хотя по-моему это ты уж слишком на будущее загадываешь. Слишком.
CREATE TABLE IF NOT EXISTS marriages
(id VARCHAR(32) PRIMARY KEY NOT NULL,//нужен идентификатор вообще или не нужен?
    human1 VARCHAR(32),
    human2 VARCHAR(32)
    date TEXT,
    type INTEGER,
    owner VARCHAR(32)
);

Места жительства точно в отдельном файле, их много...Место смерти надо? Не знаю...Да и место рождения - идентификатор.


30.04.2016.
Визуализация связей бубликатов. А то я уже запутался.

